{
  "name": "url-shortner",
  "version": "1.0.0",
  "description": "A highly performant and secure URL Shortener service built with **Node.js**, **Express**, **PostgreSQL**, and **Redis**. The project supports JWT authentication, detailed analytics tracking (clicks, devices, referrers), and asynchronous batch processing of URL shortening requests via CSV uploads.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "node-pg-migrate": "^8.0.4",
    "pg": "^8.21.0",
    "redis": "^6.0.0"
  }
}
